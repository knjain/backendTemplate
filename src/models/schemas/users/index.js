const {adminSchema} = require('./admin.schema');
const {eventManagerSchema} = require('./eventManager.schema');
const {ticketCheckerSchema} = require('./ticketChecker.schema');

module.exports = {adminSchema, eventManagerSchema, ticketCheckerSchema};
